import { createAction } from '@ngrx/store';

export const inc = createAction('[Count Component] Inc');
export const dec = createAction('[Count Component] Dec');
export const reset = createAction('[Count Component] Reset');
